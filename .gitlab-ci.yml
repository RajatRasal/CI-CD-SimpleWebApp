stages:
  - compile
  - test
  - deploy

compile:
  stage: compile
  script: mvn compile

test:
  stage: test
  script: mvn test

deploy:
  stage: deploy
  script:
    - mvn package
    - sudo pkill -f java
    - export PORT=5000
    - sh target/bin/simplewebapp
